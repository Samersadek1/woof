/** Boarding navigation and display labels (dog kennels only). */

export function boardingServiceLabel(_wing?: string | null): string {
  return "Boarding";
}

export function boardingCalendarTo(_wing?: string | null): string {
  return "/boarding";
}
