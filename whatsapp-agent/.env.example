# Railway runtime port (Railway also injects PORT automatically)
PORT=3001

# Logical session key when multiple WhatsApp clients are used
WEB_CLIENT_ID=admin-essentials-agent

# Keep local session/cache data inside the whatsapp-agent directory
WWEBJS_AUTH_PATH=.wwebjs_auth
WWEBJS_CACHE_PATH=.wwebjs_cache
